{
  "name": "llll",
  "version": "1.0.0",
  "description": "An archive of Romanian works in the public domain.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "eleventy --serve",
    "build": "rm -rf dist && eleventy",
    "deploy": "npm run build && ./deploy.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/llll-org/llll.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/llll-org/llll/issues"
  },
  "homepage": "https://github.com/llll-org/llll#readme",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1"
  }
}
